version: "3.9"

services:
  web:
    container_name: central-tickets
    image: central-tickets
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    restart: unless-stopped
