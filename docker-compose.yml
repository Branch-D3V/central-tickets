version: "3.9"

services:
  web:
    container_name: urach-video-front
    image: urach-video-front
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    restart: unless-stopped
